pipeline {
    agent {label 'BUILD'}
    stages {
        stage('vcs') {
            steps {
                git url: 'https://github.com/Narendra208/spring-petclinic.git',
                branch: 'develop'
            }
        }
    }    
}